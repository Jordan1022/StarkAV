{
  "name": "stark-av",
  "version": "0.1.0",
  "private": true,
  "description": "Starter website and source of truth for Stark AV.",
  "scripts": {
    "build": "node -e \"console.log('Static site: no build step required')\"",
    "dev": "npx serve .",
    "check": "node --check scripts/check-site.js && node scripts/check-site.js"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
